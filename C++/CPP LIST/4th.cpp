#include <iostream>

using namespace std;

// int c = 55;

int main()
{

    // ************************BUILD IN DATA TYPE**********************

    // int a, b, c;

    // cout << " Enter the value of a: " << endl;
    // cin >> a;
    // cout << " Enter the value of b: " << endl;
    // cin >> b;
    //                                     //  local operator (a, b, c)
    // c = a + b;

    // cout << " the total number is " << c << endl;
    // cout << " the global number is " <</*global operator*/::/*scope resulation operator*/c << endl;

    // ************************FLOAT, DUBLE, LONG DUBLE LETERALS**********************


    // float d = 55.5f/*(F/f)floting number*/;
    // long double g = 77.7l/*(L/l)long duble*/;

    // cout<< " the value of d is "<<d<<endl<< " the value of g is "<<g<<endl;


    // cout<< " the size of 55.5 is "<<sizeof(55.5)/*duble*/<<endl;
    // cout<< " the size of 55.5F is "<<sizeof(55.5F)/*flote*/<<endl;
    // cout<< " the size of 55.5f is "<<sizeof(55.5f)/*flote*/<<endl;
    // cout<< " the size of 77.7l is "<<sizeof(77.7l)/*long duble*/<<endl;
    // cout<< " the size of 77.7L is "<<sizeof(77.7L)/*long duble*/<<endl;


    // ************************REFERANCE VARIABLES**********************

    // float x = 88;
    // float & y = x;
    // cout<<x<<endl;
    // cout<<y;


    // ************************TYPECASTING****************************


    // int a = 44.6;
    // float b = 34.5;
    // // cout<< " the value of a is "<<(float)a<<endl;
    // // cout<< " the value of b is "<<(int)b<<endl;

    // int c = float(a);
    // cout<<" the expression is "<<a + b<<endl;   
    // cout<<" the expression is "<<a + (int)b<<endl;     
    // cout<<" the expression is "<<c + (int)b<<endl;     
    // cout<<" the expression is "<<(float)a + b<<endl;   


    return 0;
}